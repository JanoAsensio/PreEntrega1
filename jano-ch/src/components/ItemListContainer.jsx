import { Box } from "@chakra-ui/react";

/* eslint-disable react/prop-types */
const ItemListContainer = ({ content }) => {
  return <Box p="2.5rem">{content}</Box>;
};

export default ItemListContainer;
